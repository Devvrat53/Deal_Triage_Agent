# Pinned to the versions verified to work locally.
# NOTE: these agent-framework builds are recent/preview. If a host can't resolve them
# from PyPI, run `pip freeze > requirements.txt` in the working venv and use that.
agent-framework==1.9.0
agent-framework-openai==1.8.2
agent-framework-azure-ai-search==1.0.0b260521
azure-search-documents==11.7.0b2
openai==2.44.0
pydantic==2.13.4
streamlit==1.58.0
python-dotenv==1.2.2
pymupdf==1.27.2.3
python-docx==1.2.0
